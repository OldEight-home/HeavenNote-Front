
<!--
<view class="line"></view>

<view wx:if="{{!isLogin}}" class="headview">
  <view class='headImageView'>
              <image class='headImage' src="/icon/default_headicon.png"
          mode='scaleToFill'></image>
   </view>
   <view class='contentText'>登录后同步数据</view>
</view>
-->

<block wx:if="{{!isLogin}}">
  <notLogin/>
</block>
<block wx:if="{{isLogin}}" >
<!--
  <view class="search">
    <form bindsubmit="onSubmit">
      <input type="text" maxlength="25" placeholder=" 搜一下吧" /> 
    </form>
  </view>
  -->
  <view wx:for="{{noteList}}" wx:for-item="note">
    <view class="line1"></view>
  <view  class="noteInfo" bindtap="openNote" data-noteId="{{note.noteId}}" >
    <view class="title">{{note.title}}</view>
    <image class="lock" wx:if="{{!note.isPublic}}" src="../../icon/lock.png"></image>
    <view class="desc">{{note.description}}</view>
    <view class="bottomInfo">
      <view class="dateTime">{{note.updateTime}}</view>
      <image class="noteCountImg" src="../../icon/view.png"></image>
      <view class="noteCount">{{note.noteCount}}</view>
    </view>
  </view>
  <view class="line2"></view>
  </view>
</block>


